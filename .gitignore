config.json
.env
.env.*
*.tmp
exp_batch_*.json
node_modules/
coverage/
generated-*.json
*.progress.json
reference_books/

# Python
__pycache__/
*.pyc

# Untriaged IMA exam PDFs dumped at repo root with hash filenames
/[0-9][0-9][0-9][0-9][0-9][0-9]_*.pdf

# Local pre-edit snapshots of questions.json + sibling data files
data/questions.json.bak*
data/distractors.json.bak*
data/question_chapters.json.bak*
data/regulatory.json.bak*
data/syllabus_data.json.bak*

# Chaos bot run artifacts — large per-run jsonl + screenshots, regenerable
chaos-reports/v4-overnight-*/
chaos-reports/audit*/
chaos-reports/instrumented/
chaos-reports/long*/
chaos-reports/stacks/
chaos-reports/validate*/
chaos-reports/*.log
chaos-reports/*.jsonl
chaos-reports/*.json
chaos-reports/*.md
chaos-reports/*.png
chaos-reports/v4-leaderboard-*/

# FM-flavored chaos bot scripts — sibling experimentation, not Geri runtime.
# Drop from Geri tracking; these belong in FamilyMedicine if anywhere.
scripts/chaos-doctor-bot-fm.mjs
scripts/chaos-live-bot-v2-fm.mjs
scripts/fm-state-probe.mjs

# Auto-audit / triage snapshots — local-only working state
.audit_logs/
*.local-backup.json

# Bot D / Bot C generated review queues — output of source-scanner.mjs +
# gen_ai_hard_geri.mjs. Reviewer pulls accepted Qs into source files;
# the raw generated dump itself is regenerable.
source_completeness_audit_*.json
data/ai_hard_seed.geri.generated.json

# 2026-05-10: chaos-bot output + test coverage (prevent git add -A near-misses)
chaos-reports/
coverage/
