On the count: right catch, right response. A wrong number inside a report about rigor is exactly the failure a reader is least likely to check, because the surrounding claim sounds careful. Correcting it plainly rather than letting it round off is the same discipline as everything else tonight, just applied to the report's own arithmetic instead of the mechanism it's describing.

On the timing test — this is the actual result, not a failed experiment, and it's worth stating that distinction clearly since it's easy to read "the test didn't work" as a setback. You didn't fail to answer whether the body drifts. You found that the question is unanswerable by this method, because the object dies faster than the failure mode you were trying to observe. That's a stronger and more useful result than either "stable" or "drifted" would have been — a "drifted" result would have looked like a bug in the extractor, something to iterate on. "The object doesn't survive long enough to be tested" is a structural fact about the foundation, and it's the thing that makes persisting the raw text non-optional rather than one option among several. Good that you're reading it that way rather than as an inconclusive run to retry.

Finding 2 swallowing Finding 1 rather than sitting beside it is the right way to see it, and it changes what the eventual write-up should say. Not "two related instability findings, of increasing severity" — one finding, about the foundation, that made a narrower question undecidable along the way. Worth keeping that as a single entry rather than two, so nobody downstream reads it as a sequence of patches that each got a little further.

Symmetric verification taken up correctly, and naming which direction the current rule doesn't cover is the right scoping — not overclaiming it closes both directions when only one is built yet.

Persisting the fetched text at fetch time is the right next step, for the reason already given: it doesn't touch the fabrication ceiling, it closes the specific, now-proven gap that a digest becomes unrecoverable-in-principle once the source object is gone. Nothing further from me until it's built and red-tested. Holding.